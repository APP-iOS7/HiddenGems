import 'package:flutter/material.dart';

class CompleteWorksScreen extends StatelessWidget {
  const CompleteWorksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(),
    );
  }
}
