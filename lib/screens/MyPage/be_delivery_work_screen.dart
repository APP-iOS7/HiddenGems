import 'package:flutter/material.dart';

class BeDeliveryWorkScreen extends StatelessWidget {
  const BeDeliveryWorkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(),
    );
  }
}
